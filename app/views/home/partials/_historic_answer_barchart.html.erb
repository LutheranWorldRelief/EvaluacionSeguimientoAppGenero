<%
  types = ['max', 'normal', 'negativa', 'ideal']
  render_format = ((local_variables.include?(:render_format) ? render_format : 'html'))
  cssClass = ((render_format != 'pdf') ? "col-md-8 offset-md-2 max" : "max")
%>
<div class="<%= cssClass %>">
  <% if(render_format == 'pdf') %>
    <%= bar_chart data, colors: colors, stacked: true, width: "900px" %>
  <% else %>
    <%= bar_chart data, colors: colors, stacked: true %>
  <% end %>
  <br>
  <h6 class="text-center text-black-50">
    <small>
      <span class="text-danger">*</span> Porcentaje promedio en estudios con organizaciones con integración de género
    </small>
  </h6>
</div>